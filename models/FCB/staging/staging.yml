version: 2

sources:
  - name: staging
    description: "Raw staging layer views."
    tables:
      - name: v_stg_orders
        description: "Staging view for orders."
      - name: v_stg_inventory
        description: "Staging view for inventory."
      - name: v_stg_transactions
        description: "Staging view for transactions."
